# operating system and editor leftovers
.DS_Store
Thumbs.db
*~

*.log

node_modules
.npm-cache

# build output
dist
test/client.*

# test and coverage output
coverage
.nyc_output
test-results

# the machine-readable companion to the committed benchmarker/BENCHMARKS.md, rewritten by every run
benchmarker/BENCHMARKS.json

# one developer's own before-and-after reference point, which means nothing on anybody else's machine
benchmarker/BASELINE.json
