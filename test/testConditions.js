const conditions = [
  {
    test: 'should evaluate <if something> as true (conditionals/if.html)',
    template: 'if.html',
    match: '<p>The variable \'something\' is present</p>'
  }
]

module.exports = conditions
